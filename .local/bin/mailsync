#!/usr/bin/env bash

notmuch search --output=files --format=text0 tag:killed | xargs -r0 rm
mpop -a & mbsync -a -c "$HOME/.config/isync/config"
notmuch new
[ ${VAR:=$(notmuch count tag:unread)} -eq 0 ] && notify-send "No new messages." || notify-send "You have $VAR unread messages."
